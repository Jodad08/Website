{
  "name": "josiahjardine",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:pages": "wrangler pages deploy public --project-name=josiahjardine",
    "images:localize": "node scripts/localize-images.mjs"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
